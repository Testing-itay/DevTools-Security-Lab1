---
name: Review
description: Review a diff for security regressions before merge.
---

# Review

Security review. Owned by `code-guardian`.

The same skill path exists under `plugins/sds/`. Both share the `plugins/`
first segment, so a first-segment rule cross-links them.
