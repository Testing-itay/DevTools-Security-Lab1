---
name: Schema Draft
description: Draft a request and response schema for a new endpoint.
---

# Schema Draft

Owned by `py-plugin-endpoint-designer`, the inner plugin root —
not by the `python-api-plugin` bundle that contains it.
