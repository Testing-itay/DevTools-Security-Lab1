# sds-frontend-plugin

Frontend bundle: SDS form agents and accessibility skills.

Bundle-level manifest. The agents under `agents/` carry
their own manifests, so this tree yields one entity per level.
